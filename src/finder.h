#ifndef FINDER_H
#define FINDER_H

// Returns the index of the first element of `values` (of length `size`) equal
// to `target`, or -1 if no such element exists.
int findValue(const int* values, int size, int target);

#endif // FINDER_H
